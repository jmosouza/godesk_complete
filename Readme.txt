# Godesk Project

The most important classes have comments.
Ruby documentation can be generated from the comments with RDoc.

Run tests with `rspec` command

Annotate model info with `annotate` command
